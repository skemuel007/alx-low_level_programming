#include"main.h"

/**
 * _puts - Function name
 *
 * @str: string parameter of type s
 *
 * Return: Nothing
*/
void _puts(char *str)
{
	int counter;

	for (counter = 0; *str != '\0'; ++s)
	{
		_putchar(*str + 0);
		counter++;
	}
	_putchar('\n');
}
